#ifndef PRIMARY_CAPS_H
#define PRIMARY_CAPS_H

#include "constants.h"
#include "hls_stream.h"

void process_features(hls::stream<float> stream_conv_s[FILTERS], hls::stream<float> stream_primary_caps_s[CAPSULES])

#endif	// PRIMARY_CAPS_H