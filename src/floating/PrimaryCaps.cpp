#include "PrimaryCaps.h"

static void conv2d(hls::stream<float> &stream_conv_s, hls::stream<float> &stream_primary_caps_s);

static void conv2d(hls::stream<float> &stream_conv_s, hls::stream<float> &stream_primary_caps_s)
{
}

void process_features(void process_features(hls::stream<float> stream_conv_s[FILTERS], hls::stream<float> stream_primary_caps_s[CAPSULES]))
{
}